<!DOCTYPE html>
<html>
<head>
    <title>{{ $titre }}</title>
</head>
<body>
    <h1>{{ $titre }}</h1>
    <p>{{ $contenu }}</p>
    <p><small>Type : {{ $type }} — {{ $date }}</small></p>
</body>
</html>